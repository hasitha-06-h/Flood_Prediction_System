__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# SQLite database - generated at runtime, shouldn't be committed
database/*.db

# Model artifacts are regenerated by train_model.py - comment these two
# lines out if you'd rather ship a pre-trained model inside the repo.
models/*.save
models/model_metadata.json

reports/*.png
reports/training_summary.txt

.DS_Store
*.log
.idea/
.vscode/
